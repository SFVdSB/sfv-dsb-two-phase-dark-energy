# Publication claim boundary — v1.0.0

## Derived or controlled results

- Published gravitational O(4) saddle admits the retained thick `dS3` wall continuation.
- The global wall radius/area provides a relational background history coordinate.
- For supplied `N_star`, the handoff radius is fixed by `R_star = H3^-1 exp(N_star)`.
- Under the declared maximal-depth convention, `L_IR = sqrt(3) Mbar_4 R_star^2`.
- The late density then obeys exactly `rho_DE = R_star^-4 = Lambda_global^4`.
- Constant global/extrinsic `dS4` curvature gives exact effective `w=-1`.
- The late term is negligible through the hot early universe under the frozen ownership rules.
- A universal geometric handoff has broad visible-loading capacity without a fitted reheating efficiency.
- A flavor-blind factorized handoff preserves the frozen Phase-B2 internal flavor invariants.

## Conditional / phenomenological elements

- `N_star = 57.8401844744` is one continuous phenomenological history datum.
- Maximal-depth saturation is a discrete holographic reconstruction postulate, not a theorem derived from the full action.
- The inverse-radius screen ruler is the current order-one normalization convention.
- `{A_s,n_s}` are phenomenological prepared primordial-state parameters.
- Exact channel-resolved reheating fractions are not predicted.
- Phase-B2 survival across handoff assumes the leading geometric transition is internal-flavor blind.

## Explicitly excluded claims

- The action alone predicts today's absolute dark-energy density.
- Ordinary CKN with the microscopic `F` scale predicts a cosmic IR horizon.
- `H8` or `Lambda_mem` are dark-energy ingredients; they remain DM/memory-only.
- The global wall `S2` radius is inherited as physical FRW `k=+1` curvature.
- The diagnostic `C=0` scalar-potential zero is a post-handoff observed vacuum source.
- Emergent relational time by itself derives the CMB scalar spectrum.
- Historical `r ~ 0.015` is a frozen prediction.
- The wall rotates or the model contains observable global vorticity.

## Reopening rule

The two-phase architecture is frozen. It should be reopened only if a future derivation produces an internal contradiction or a robust empirical result falsifies a frozen prediction/condition.

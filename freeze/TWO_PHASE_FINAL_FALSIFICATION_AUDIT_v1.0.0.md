# Two-phase final falsification / claim-boundary audit — v1.0.0

## Verdict

**PASS FOR A FROZEN CONDITIONAL THEORY.**

No fatal contradiction was found after the final hostile end-to-end audit, provided every conditional ownership rule is stated explicitly and the non-FP elements are not presented as derived predictions.

This is **not** a first-principles closure verdict. It is a self-consistent conditional-theory closure verdict.

## Gate 1 — primitive / postulate ledger

PASS. The non-FP burden is compact and visible:

- one continuous cosmological history datum: `N_*`;
- two phenomenological primordial-state parameters: `{A_s,n_s}`;
- a finite set of discrete holographic/metric-ownership assumptions.

No hidden continuous reheating efficiency, DE coefficient, inflaton parameter, scalar clock mass, or CMB normalization parameter beyond `{A_s,n_s}` was found.

## Gate 2 — energy double counting

PASS with frozen ownership rules.

Hostile alternative: treat wall tension, transition energy, Brown–York energy and late `dS4` curvature as additive FRW sources.

Result: this fails and is excluded. The viable model assigns them to different descriptions/epochs. In particular the same boundary/bulk handoff energy is never added twice.

## Gate 3 — metric ownership

PASS conditionally.

Hostile alternative A: inherit the global `S2` wall radius as physical `k=+1` FRW curvature.

Result: NO-PASS; curvature becomes unacceptable before recombination. Frozen theory uses the local flat causal patch.

Hostile alternative B: demand continuity `H4=H3` across handoff.

Result: rejected because the large early `H3` is primarily extrinsic wall acceleration. Observable expansion ownership changes at reconstruction.

## Gate 4 — vacuum reference

PASS conditionally.

Hostile alternative: insert the diagnostic `C=0` true-vacuum potential directly into reconstructed FRW.

Result: fatal early recollapse. Frozen theory instead uses the inherited true-Minkowski gravitational reference `C=-V_T` and couples the reconstructed metric to excitation/Brown–York energy above that reference.

This is a load-bearing boundary/renormalization condition and must not be described as an automatically derived cancellation of vacuum energy.

## Gate 5 — hot-era contamination

PASS.

The late `L_IR^{-2}` term is negligible through BBN and recombination. No late-time switch-off/on switch is required; ordinary radiation/matter dilution makes the global IR geometry relevant only in the deep IR.

## Gate 6 — reheating / visible loading

CONDITIONAL PASS.

Hostile alternative: the handoff dumps essentially all energy into a permanently decoupled hidden Goldstone bath.

Result: not favored by the universal stress-weight comparator and would be constrained by dark radiation. The frozen geometric handoff has nonzero universal access to all open stress-carrying sectors, with the visible sector dominating the available spectral weight in controlled degree-of-freedom comparators.

Exact absolute branching and reheating temperature remain future derivation targets and are not claimed as predictions.

## Gate 7 — flavor

CONDITIONAL PASS.

A universal geometric handoff factorizes from internal flavor space and therefore preserves frozen Phase-B2 internal singular-value ratios and relative family rotations while changing localization residues. A species-dependent handoff would invalidate this conclusion and is excluded unless independently derived.

## Gate 8 — DM / memory

CONDITIONAL PASS.

`H8` and `Lambda_mem` remain DM/memory-only. The handoff must be delocalizing rather than destructive: the microscopic screen remains the holographic boundary. If future derivation requires literal destruction of the screen, the retained DM/memory module must be re-audited.

## Gate 9 — late screen depth / circularity

PASS only as an explicit conditional relation.

Hostile alternative: call the `16.7 Gly` scale an ordinary CKN prediction obtained from the microscopic matter cutoff `F`.

Result: false. Ordinary CKN applied to `F` gives a microscopic IR scale. The viable frozen claim is narrower: after Wilsonian/DtN separation the **global** screen ruler `R_*` owns the nonlocal reconstruction sector, and a maximal-depth CKN-shaped reconstruction law maps it to `L_IR`.

Because `N_*` is not independently derived in v1.0.0 and is fixed by the late scale, the absolute DE normalization is conditional/phenomenological rather than a blind prediction. The exact `w=-1` result remains geometric conditional on `L_IR` ownership.

This explicit labeling removes the future-boundary/circularity problem from the claim language: the model does not claim to predict the observed DE normalization from action primitives in v1.0.0.

## Gate 10 — primordial perturbations

PASS only after explicit phenomenological quarantine.

The current action does not derive the observed adiabatic scalar amplitude/tilt. The frozen model adopts the minimal prepared Gaussian state `{A_s,n_s}`. This is not described as a prediction.

The exact Goldstone `|q|^3` trace shape, emergent relational time, O(4) flow diagnostics and 5D scalar projector are retained as future derivation clues, not used to fake closure.

## Gate 11 — hidden fourth continuous parameter

PASS. No additional continuous cosmological parameter is required beyond `N_*`, `A_s`, and `n_s` at the present level of closure.

The exact screen-depth order-one convention is discrete, and exact reheating partition is left unpredicted rather than promoted to an efficiency parameter.

## Gate 12 — material-flow interpretation

NOT PART OF THE FROZEN PREMISES.

A stationary wall plus nontrivial 4D/5D material or superfluid flow around/through the interface is conceptually compatible with the user's preferred physical ontology and may be important to a future microscopic completion. However:

- wall rotation is not assumed;
- literal tangential circulation would need an isotropy/symmetry audit;
- the symmetry-safe starting point is a conserved normal/radial or higher-dimensional flow field whose stress leaves the wall geometry stationary;
- no present result is reclassified as derived from such flow.

This idea is preserved as a future derivation program rather than inserted retroactively.

## Final audit disposition

The model passes the publication-style claim-boundary test **as a conditional theory** because:

1. its background chronology is internally self-consistent;
2. known hostile alternatives fail in understood ways and are explicitly excluded;
3. all remaining non-FP elements can be stated compactly;
4. retired historical claims are quarantined;
5. no hidden tuning is disguised as derivation.

The theory is therefore frozen at v1.0.0. A publication paper may be prepared in a later step if the user authorizes it, using the claim classes in the final ledger verbatim.

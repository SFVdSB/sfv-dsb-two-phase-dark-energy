# SFV/dSB two-phase holographic cosmology — theory freeze v1.0.0

**Freeze date:** 2026-08-23  
**Status:** FROZEN CONDITIONAL THEORY  
**Publication paper:** not generated in this package.

## 1. Frozen architecture

The model is frozen in the following two-phase cosmological form:

1. **O(4) nucleation** produces the literal gravitational wall.
2. **Early epoch:** the analytically continued wall is intrinsic/extrinsic `dS3`; its geometry supplies primordial accelerated expansion.
3. **Relational time:** the expanding wall radius/area provides a global semiclassical history parameter. It counts progress after nucleation but does not by itself select the handoff event.
4. **Handoff:** at the single conditional history datum
   `N_* = 57.8401844744`, the observable description changes from localized `2+1` wall physics to reconstructed local `3+1` physics.
5. **Reheating / particle release:** delocalization opens the `3+1` continuum. A universal geometric quench gives nonzero access to all open stress-carrying sectors; visible loading is conditionally dominant. The already-existing Phi amplitude channel rapidly dissipates once loaded.
6. **Hot era:** the reconstructed universe is a locally flat `k=0` FRW patch sourced by renormalized excitation/Brown–York energy above the true-vacuum reference.
7. **Late epoch:** the early handoff fixes a global holographic reconstruction scale. The corresponding global/extrinsic `dS4` geometry becomes dynamically relevant only in the deep IR and gives exact effective `w=-1`.
8. **Primordial scalar state:** the current action does not derive the observed adiabatic scalar normalization. The frozen model therefore adopts the minimal prepared Gaussian curvature spectrum with phenomenological state parameters `{A_s,n_s}`.

Compactly:

`O4 -> dS3 early acceleration -> N_* handoff -> 3+1 reheating -> radiation/matter -> global/extrinsic dS4 (w=-1)`

## 2. Frozen late-curvature normalization

With the current conditional handoff datum and reconstruction convention:

- `R_* = 85.993 micrometers`
- `R_*^{-1} = 2.294686 meV`
- `L_IR = 1.580518e26 m = 16.7061 Gly`
- `H_late = 1.24849563e-42 GeV`
- `Lambda_eff = 3 H_late^2 = 4.67622402e-84 GeV^2`
- `rho_late = 3 Mbar_4^2 H_late^2 = 2.77263843e-47 GeV^4`
- `rho_late^(1/4) = 2.294686 meV`

The absolute late scale is **not an FP prediction in v1.0.0**, because `N_*` is retained as a conditional continuous history datum and the exact maximal-depth screen-to-bulk normalization remains a discrete holographic postulate. The exact `w=-1` property is geometric once the late `dS4` ownership is granted.

## 3. Metric and energy ownership rules

These rules are part of the frozen theory because violating any one of them creates a known contradiction.

### 3.1 Early metric ownership

The large early `H3` belongs to the literal wall's intrinsic/extrinsic `dS3` geometry. It is not carried into the post-handoff Friedmann equation as a `3+1` vacuum energy.

### 3.2 Flat-patch reconstruction

The global closed `S2` slicing radius of `dS3` is not promoted to a conserved `k=+1` FRW curvature radius. The observable reconstructed `3+1` hot era is inherited from a local flat causal patch and is treated as `k=0` at the background level.

### 3.3 True-vacuum reference

The reconstructed metric couples to physical excitation/Brown–York energy above the true vacuum. The inherited gravitational reference is the true-Minkowski normalization `C=-V_T`; inserting the diagnostic `C=0` absolute scalar-potential zero into FRW is explicitly excluded.

### 3.4 No energy double counting

The model does not add wall tension/pressure energy, Brown–York excitation energy, and late extrinsic curvature as three copies of one source:

- wall tension/pressure owns the early wall geometry;
- excitation energy owns the post-handoff hot FRW stress tensor;
- global extrinsic reconstruction geometry owns the late constant curvature.

Dual boundary/bulk descriptions of the same handoff energy are not summed.

## 4. Handoff / reheating disposition

The wall thickness fixes the quench timescale; no independent transition-duration parameter is introduced. Spectral completeness guarantees that a disappearing localized state must reappear in the open continuum, but the exact channel-resolved Bogoliubov partition is not derived.

Under the frozen **universal geometric handoff** assumption,

`delta H = (1/2) int delta gamma_{mu nu} T^{mu nu}`,

inclusive production is internal-flavor blind. Stress-spectral-weight controls show the visible sector naturally dominates over one to three light hidden scalar channels at handoff energies. The exact reheating temperature is not claimed as a prediction.

The handoff acts as `U_geom tensor I_internal` on the frozen flavor structure, preserving internal mass-ratio and CKM-mismatch invariants while allowing localization residues to become continuum residues.

## 5. Perturbation disposition

The current-action search is closed for a derived primordial adiabatic scalar:

- the exact Goldstone loop gives the correct nonlocal `|q|^3` shape but the wrong normalization class;
- the relational area clock is a genuine global background clock but has zero physical local tree-level scalar response;
- the physical O(4) normal matter flow is too weak and too narrow;
- the 5D parent graviton has a scalar projector but the derived codimension-two kernel is logarithmic/profile-sensitive rather than universal `|q|^3`;
- no physical radion/second-boundary scalar exists in the frozen ontology.

Therefore v1.0.0 adopts

`Delta_R^2(k) = A_s (k/k_*)^(n_s-1)`

as a prepared Gaussian adiabatic initial state with phenomenological `{A_s,n_s}`. Running is set to zero only as a minimal leading truncation, not as a prediction. No tensor-to-scalar ratio is frozen as a prediction.

## 6. Dark sector / memory bookkeeping

`H8` and `Lambda_mem` are **DM/memory-only** quantities. They are not dark-energy ingredients in the frozen two-phase model.

The microscopic screen must persist as the holographic boundary through the handoff if the retained DM/memory branch is to survive. A destructive handoff that removes the screen is excluded from the frozen ontology.

## 7. Primitive / conditional burden

### Action / previously frozen microphysical structure

Retained from the established SFV/dSB program: O(4) bounce/wall action, gravitational continuation, Phase-B2 flavor structure, existing field/portal content, and separately retained DM/memory module.

### Conditional continuous datum

- `N_* = 57.8401844744` — handoff history datum.

### Phenomenological primordial-state data

- `A_s`
- `n_s`

### Discrete / structural conditional assumptions

- flat causal-patch `2+1 -> 3+1` metric ownership;
- true-Minkowski gravitational reference for reconstructed vacuum energy;
- delocalizing but non-destructive holographic handoff;
- universal geometric leading handoff coupling / internal-flavor blindness;
- global screen-scale ownership of the nonlocal reconstruction sector;
- maximal-depth screen-to-bulk reconstruction convention and its current order-one normalization;
- persistence of the microscopic screen for the DM/memory branch.

No new inflaton, dark-energy scalar, slow-clock field, reheating efficiency, large-N CMB sector, or new continuous dark-energy coefficient is introduced.

## 8. Freeze rule

This architecture is now **frozen**. Future work is permitted to:

1. derive currently conditional items from the existing or minimally completed substrate;
2. tighten predictions;
3. falsify the frozen model.

Future work should **not** redesign the architecture merely to improve elegance or recover an FP label. The architecture is reopened only if a derivation exposes an internal contradiction or a decisive empirical falsification.

# Publication readiness — frozen theory v1.0.0

The final falsification/claim-boundary audit passes for publication **as a conditional theory**, provided the paper uses the final claim ledger and does not overstate conditional quantities as first-principles predictions.

The theory is now frozen. The publication paper and clean GitHub/Zenodo repository have **not** been generated in this package. They should be produced only on explicit user instruction.

Any publication must state prominently:

- the two-phase geometry is the central model result;
- `N_*` is currently conditional/phenomenological;
- `{A_s,n_s}` are prepared primordial-state parameters;
- the exact late `w=-1` result is geometric, but the absolute late scale is conditional on the handoff/reconstruction relation;
- H8/`Lambda_mem` are DM/memory-only;
- reheating/flavor closure is conditional on a universal geometric handoff;
- the material-flow completion is future work, not part of the current derivation.
